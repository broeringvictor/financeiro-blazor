@page "/contas"
@attribute [Microsoft.AspNetCore.Authorization.Authorize]
@inject NavigationManager Navigation

@code {
    // Rota legada: "Contas" virou "Lançamentos" (entradas + saídas). Mantém bookmarks antigos funcionando.
    protected override void OnInitialized() => Navigation.NavigateTo("/lancamentos", replace: true);
}
