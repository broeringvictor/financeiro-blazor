@page "/contas"
@attribute [Microsoft.AspNetCore.Authorization.Authorize]
@rendermode InteractiveServer
@using WebApp.Components.Shared

<PageTitle>Contas</PageTitle>

@if (!string.IsNullOrEmpty(_userId))
{
    <ContasGrid UserId="@_userId" />
}
