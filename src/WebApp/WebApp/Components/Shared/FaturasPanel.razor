@if (!string.IsNullOrEmpty(UserId))
{
    <InvoicesGrid @ref="_grid"
                  UserId="@UserId"
                  BillId="@BillId"
                  OnPay="PagarFatura"
                  OnCancel="CancelarFatura"
                  OnDelete="ExcluirFatura" />
}
